# 避難所検索機能を実装するためのプロンプト

以下の要件を満たす避難所検索機能を、現在のプロジェクトの技術スタック・画面構成・命名規則に合わせて実装してください。
既存機能を壊さず、検索機能に必要なファイルだけを変更してください。実装後、変更内容と確認方法を簡潔に報告してください。

## 目的

避難所を条件で検索できる画面を作る。検索画面を開いた時点で、利用可能な避難所一覧を画面下部に表示し、入力や条件変更に応じて一覧を即時更新する。

## 必須画面

### 1. 避難所検索画面

検索画面に次の要素を配置する。

- ページタイトル: 「避難所検索」
- 説明文: 「条件を指定して、利用できる避難所を探せます。」
- 施設名入力欄
  - ラベルは「施設名」
  - プレースホルダーは「施設名を入力または選択」
  - 登録済み施設名を候補として表示できる datalist または同等の補完UIを使う
- 距離の選択欄
  - 「指定なし」
  - 「500m以内」
  - 「1km以内」
  - 「3km以内」
  - 「5km以内」
- 並び順の選択欄
  - 「施設名順」
  - 「近い順」
  - 「収容人数順」
- 対応条件のチェックボックス
  - 「津波対応」
  - 「ペット可」
  - 「車いす対応」
  - 「備蓄あり」
- 検索画面の右側または適切な位置に、地図から探す領域を表示する
  - 見出しは「地図から探す」
  - 対象地域名を表示する
  - 登録施設をマーカーで表示する
- 画面下部に「避難所一覧」を表示する
  - 一覧見出しの横に現在の件数を「○施設」形式で表示する
  - 初期状態では全施設を表示する
  - 該当施設がない場合は「条件に合う避難所がありません。」を表示する

## 検索動作

- 施設名は入力文字列を含む施設を部分一致で絞り込む。
- 施設名検索は大文字・小文字の違いを吸収する。日本語入力でも正しく動作させる。
- 距離が指定された場合、施設の距離が上限以内の施設だけを表示する。
- 津波対応を選択した場合、津波対応施設だけを表示する。
- ペット可を選択した場合、ペット可施設だけを表示する。
- 車いす対応を選択した場合、車いす対応施設だけを表示する。
- 備蓄ありを選択した場合、備蓄がある施設だけを表示する。
- 複数条件を選択した場合は、すべての条件を満たす施設だけを表示する。
- 条件変更時にページ全体をリロードせず、施設一覧と件数を即時更新する。
- 施設名順は日本語の施設名順で並べる。
- 近い順は距離の昇順で並べる。距離未登録の施設は最後に置く。
- 収容人数順は収容人数の降順で並べる。
- フォーム送信による不要なページ遷移は避け、検索画面内で結果を更新する。

## 施設名入力時の情報吹き出し

- 画面を開いた直後は吹き出しを表示しない。
- 施設名欄に登録済み施設の名前が完全一致で入力・選択された場合だけ、入力欄の直下に吹き出しを表示する。
- 部分一致、空欄、未登録の名前の場合は吹き出しを非表示にする。
- 吹き出しには次の情報を表示する。
  - 施設名
  - 収容人数
  - 津波対応状況
  - ペット対応状況
  - 車いす対応状況
  - 備蓄状況
- 吹き出し内の施設名をクリックすると、その施設の詳細ページへ移動する。
- HTMLの `hidden` 属性を使う場合、CSSの `display` 指定で初期表示されないようにする。

## 地図連動

- 地図上の各マーカーに施設名と概要を表示できるようにする。
- マーカーのポップアップには施設名、収容人数、対応状況、詳細ページへのリンクを表示する。
- ポップアップ内に「この施設だけ表示」操作を用意する。
- その操作を選択した場合、施設名欄に施設名を設定し、吹き出しを表示し、一覧をその施設だけに絞り込む。
- 地図ライブラリを既存プロジェクトで使っている場合は、それを優先して使う。新規導入が必要な場合は依存関係と読み込み方法を明示する。

## 検索結果ページ・詳細表示

サーバー側の検索結果ページが既にある場合は、検索条件をクエリパラメータで受け取れるようにする。

- 施設名: `q`
- 最大距離: `max_distance`
- 津波対応: `tsunami=1`
- ペット可: `pets=1`
- 車いす対応: `barrier_free=1`
- 備蓄あり: `supplies=1`
- 並び順: `sort`

検索結果ページでは、各施設を施設詳細ページへのリンクとして表示する。
施設名にカーソルを合わせたときは、施設の詳細情報を吹き出しで表示できるようにする。

ただし、ホバー時の背景色やリンク色は濃すぎない、淡く落ち着いた色にする。検索画面と検索結果画面の施設リンクに、強い青色の背景を付けない。通常時の文字色も視認性を保ちながら濃すぎない色にする。

## 施設データ

既存の施設データ構造がある場合はそれを利用し、項目名に差異がある場合は既存データとの互換性を保つ。
最低限、次の情報を扱えるようにする。

```json
{
  "id": 1,
  "name": "施設名",
  "distance_m": 500,
  "capacity": 100,
  "tsunami": true,
  "pets": true,
  "barrier_free": true,
  "emergency_supplies": true,
  "latitude": 40.8244,
  "longitude": 140.7400
}
```

距離、収容人数、位置情報、対応条件が未登録でも画面が壊れないようにする。未登録値は「未登録」と表示する。

## 不要な機能を追加しない

- 検索画面には、すでに画面下部に施設一覧が表示されるため、「条件を指定せず全施設を見る」「全施設を表示」などの重複リンク・ボタンを追加しない。
- 検索機能と無関係な画面、認証、指示ボード、天気情報などは変更しない。
- 既存の施設詳細ページへの導線は維持する。

## アクセシビリティとレスポンシブ対応

- すべての入力欄に適切なラベルを付ける。
- ボタンやリンクはキーボード操作でも利用できるようにする。
- 吹き出しはマウスだけでなくフォーカス時にも確認できるようにする。
- スマートフォンでは検索条件が縦に並び、施設一覧と地図が画面からはみ出さないようにする。
- 動的に更新される件数と結果一覧には、必要に応じて `aria-live` を設定する。

## 実装後の確認項目

次を実際に確認する。

1. 検索画面を開いた直後、全施設一覧が表示され、施設名吹き出しは表示されない。
2. 施設名を入力すると部分一致で一覧が絞り込まれる。
3. 登録済み施設名を完全一致で入力すると吹き出しが表示される。
4. 施設名を消す、または未登録名に変更すると吹き出しが消える。
5. 距離、並び順、4種類の対応条件がそれぞれ動作する。
6. 条件を組み合わせたとき、すべての条件が適用される。
7. 地図マーカーと「この施設だけ表示」が動作する。
8. 施設名または詳細リンクから施設詳細ページへ移動できる。
9. 検索結果のホバー色が濃すぎず、文字や吹き出しが読みやすい。
10. 該当件数が0件のとき、一覧が空になりメッセージが表示される。
11. スマートフォン幅でもレイアウトが崩れない。

実装前に既存のルーティング、施設データ、共通レイアウト、CSS、JavaScriptを確認し、既存のパターンに合わせて最小限の変更で実装してください。
